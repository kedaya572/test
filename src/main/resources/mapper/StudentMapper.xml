<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.mapper.StudentMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student (name, gender, age, student_no, major, email, phone)
        VALUES (#{name}, #{gender}, #{age}, #{studentNo}, #{major}, #{email}, #{phone})
    </insert>

    <update id="update">
        UPDATE student
        SET name=#{name}, gender=#{gender}, age=#{age}, student_no=#{studentNo},
            major=#{major}, email=#{email}, phone=#{phone}
        WHERE id=#{id}
    </update>

    <delete id="deleteById">
        DELETE FROM student WHERE id = #{id}
    </delete>

    <select id="findById" resultType="com.xxx.entity.Student">
        SELECT * FROM student WHERE id = #{id}
    </select>

    <select id="findByStudentNo" resultType="com.xxx.entity.Student">
        SELECT * FROM student WHERE student_no = #{studentNo}
    </select>

    <select id="findAll" resultType="com.xxx.entity.Student">
        SELECT * FROM student
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="major != null and major != ''">
                AND major LIKE CONCAT('%', #{major}, '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM student
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="major != null and major != ''">
                AND major LIKE CONCAT('%', #{major}, '%')
            </if>
        </where>
    </select>

</mapper>